{
    "data_dir": "epfl_augmented/",
    "training_sets": ["train/images", "train/groundtruth"],
    "validation_sets": ["validation/images", "validation/groundtruth"],

    "dataset_image_size": [384, 384],

    "train_batch_size": 8,
    "validation_batch_size": 8,

    "train_max_epoch": 30,
    "train_init_lr": 1e-4,
    "train_min_lr": 1e-7,
    "train_save_dir": "saved_model/best_model.pth",

    "comment": "Updated config for new dataset without resizing, patching, or overlaps.",
    "deployment_batch_size": 1
}
